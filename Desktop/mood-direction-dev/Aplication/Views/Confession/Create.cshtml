@{
    ViewData["Title"] = "Не мудак ли я? — Новый вопрос";
}

<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <h5>❓ Не мудак ли я?</h5>
            <p class="text-muted small mb-0">
                Опишите ситуацию честно. Люди помогут взглянуть со стороны.
            </p>
        </div>
        <div class="card-body">
            <form method="post">
                <div class="mb-3">
                    <label class="form-label">Заголовок (необязательно)</label>
                    <input name="title" class="form-control" 
                           placeholder="Например: «Я обманул друга ради пользы»" />
                </div>

                <div class="mb-3">
                    <label class="form-label">Ваша дилемма <span class="text-danger">*</span></label>
                    <textarea name="content" class="form-control" rows="6" required
                              placeholder="Что случилось? Что вы сделали? В чём сомневаетесь?"></textarea>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Опубликовать</button>
                    <a href="@Url.Action("Index", "Confession")" class="btn btn-secondary">Отмена</a>
                </div>
            </form>
        </div>
    </div>
</div>